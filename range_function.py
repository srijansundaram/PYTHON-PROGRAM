for i in range(1,8,3): # by default it starts from 0. if we want it to start lets say 'x' tp 'y', we can write (x, y ).
    print(i)


#  step size: 
    # (x, y, z)
    # x = start y = stop z = step size which basically skips digit as per your choice